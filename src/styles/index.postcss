@import './color.postcss';
@import './md.postcss';


* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

#app {
  height: 100vh;
  min-height: 100vh;
  max-height: 100vh;
  overflow: auto;
  background-color: var(--liam-bg);
  color: var(--liam-text);
}
